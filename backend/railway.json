{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn fbmanager.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
  }
}