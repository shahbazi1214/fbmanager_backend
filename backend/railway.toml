[build]
builder = "RAILPACK"

[deploy]
startCommand = "gunicorn fbmanager.wsgi:application --bind 0.0.0.0:$PORT"

[deploy.preDeployCommand]
command = "python manage.py migrate && python manage.py collectstatic --noinput"